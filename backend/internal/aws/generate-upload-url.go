package aws

import "github.com/Aboody-Studios/ballr/backend/internal/models"

func GenerateUploadURL(video models.Video) (string, error) {
	// generate s3 upload url here when we have an amazon account
	return "lol", nil
}
